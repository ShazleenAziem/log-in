function login()
{
    let password = document.getElementById("password").value;
    if(password.length <= 12)
    {
        alert("not enough")
    }
        else
    {
        alert("password");
    }
    
}